import React from 'react';
import styled from 'styled-components';

const Loader = () => {
  return (
    <div className="section">
      <h2 className="section-center">⏳</h2>
    </div>
  );
};

export default Loader;
